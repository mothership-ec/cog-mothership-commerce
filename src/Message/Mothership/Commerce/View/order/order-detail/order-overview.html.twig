{% extends '::order:order' %}

{% block order_content %}
	<div class="container-content">
		<section class="repeatable-group">
			{{ render(controller('::Controller:Order:Order#orderDetails', {'orderId': order.id } )) }}
		</section>

		<section>
			{{ render(controller('::Controller:Order:Item#itemSummary', { 'orderId': order.id } )) }}
		</section>

		<section>
			{{ render(controller('::Controller:Order:Address#addressSummary', { 'orderId': order.id } )) }}
		</section>
	</div>
{% endblock %}