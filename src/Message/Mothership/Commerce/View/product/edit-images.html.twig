{% extends 'Message:Mothership:ControlPanel::_templates/left_sidebar' %}

{% block sidebar %}
	{{ render(controller('Message:Mothership:Commerce::Controller:Sidebar#index', {'productID':product.id}))}}
{% endblock %}

{% block main %}

<hgroup class="title">
		<h1>Editing Stock'</h1>
</hgroup>

{{ render(controller('Message:Mothership:Commerce::Controller:ProductsMenu#index', {'productID':product.id}))}}

<section class="container-products">
<ul>
	{% for image in product.images %}
		<li>
			<img src="{{ resize(image.file.file.getPublicUrl, 200, 200) }}" />
			<ul>
				<li>{{ image.type }}</li>
				<li>{{ image.optionName }}</li>
				<li>{{ image.optionValue }}</li>
			</ul>
		</li>
	{% endfor %}
</ul>
	{{ form_start(form) }}
		{{ form_rest(form) }}
		<button type="submit" class="button small save" id="save-content">Update</button>
	{{ form_end(form) }}

</section>

{% endblock %}
